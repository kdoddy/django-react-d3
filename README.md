## How to run locally

### backend:

cd django-todo-react
pip install pipenv
pipenv shell
cd backend
python manage.py runserver

### frontend:

cd django-todo-react/frontend
npm install
yarn start or npm start